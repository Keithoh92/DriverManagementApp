<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FindDriversActivity"
    android:background="@drawable/background_colour_no_radius">

    <include
        android:id="@+id/toolbar_find_drivers"
        layout="@layout/toolbar_management"
        >
    </include>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/find_drivers_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar_find_drivers"/>

</RelativeLayout>